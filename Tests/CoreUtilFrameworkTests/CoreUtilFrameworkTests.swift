import XCTest
@testable import CoreUtilFramework

final class CoreUtilFrameworkTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CoreUtilFramework().text, "Hello, World!")
    }
}
